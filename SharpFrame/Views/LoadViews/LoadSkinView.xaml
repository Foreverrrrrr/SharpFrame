<UserControl x:Class="SharpFrame.Views.LoadViews.LoadSkinView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SharpFrame.Views.LoadViews"
             mc:Ignorable="d" 
             xmlns:prism="http://prismlibrary.com/" 
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="Transparent" Height="{Binding Height}" Width="{Binding Width}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Border Background="#FF6B6B6B" Opacity="0.5">
        </Border>
        <Grid>
            <ProgressBar Width="100" Height="100" Style="{StaticResource MaterialDesignCircularProgressBar}" Value="0" IsIndeterminate="True" Background="{x:Null}" BorderBrush="{x:Null}" />
        </Grid>
    </Grid>
</UserControl>
